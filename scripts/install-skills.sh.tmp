#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
TOOL=""
TARGET="."
GLOBAL=0
for arg in "$@"; do
  case "$arg" in
    --global) GLOBAL=1 ;;
    claude|cursor|codex|agents) TOOL="$arg" ;;
    *) TARGET="$arg" ;;
  esac
done
